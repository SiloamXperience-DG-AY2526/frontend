# EXPRESS_PORT: the port your Express server is listening on
# API_ROOT: the root API endpoint **CHANGE IF API VERSION CHANGES**

# BACKEND_URL: Used by server-side API routes (runs on the server)
# BACKEND_URL=http://localhost:<EXPRESS_PORT>/<API_ROOT>
BACKEND_URL=http://localhost:3000/api/v1

# NEXT_PUBLIC_BACKEND_URL: Used by client-side code (runs in the browser)
# Must be prefixed with NEXT_PUBLIC_ to be accessible in the browser
# Should be set to the same value as BACKEND_URL in development
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000/api/v1